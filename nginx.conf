location / {
    # 1. Esto permite CARGAR el archivo si entran a /contacto
    try_files $uri $uri/ $uri.html $uri.php?$query_string;
}

# 2. Esto REDIRIGE si el usuario escribe .html o .php manualmente
if ($request_uri ~ ^/(.*)\.(html|php)) {
    return 301 /$1;
}